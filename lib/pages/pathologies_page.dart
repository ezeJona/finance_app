import 'package:flutter/material.dart';

class PathologiesPage extends StatelessWidget {
  const PathologiesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Resumen de patologías'));
  }
}
